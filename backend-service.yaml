apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: joke-app
    tier: backend
spec:
  selector:
    app: joke-app
    tier: backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP # Internal service